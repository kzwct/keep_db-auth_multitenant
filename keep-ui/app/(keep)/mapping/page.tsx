import Mapping from "./mapping";

export default function Page() {
  return <Mapping />;
}

export const metadata = {
  title: "WeCanTest - Event Mapping",
  description: "Add dynamic context to your alerts with mapping",
};
